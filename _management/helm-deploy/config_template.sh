#!/bin/bash
# Any secrets should be passed in from config management
subscription_id=[replace with azure subscription id]
client_id=[replace with azure client_id]
client_secret=[replace with azure client_secret]
tenant_id=[replace with azure tenant_id]